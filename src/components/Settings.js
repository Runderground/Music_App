import React from 'react';
import '../styles/Settings.css';

const Settings = () => {
    return (
        <div className="settings">
            <h2>Configurações</h2>
        </div>
    );
};

export default Settings;
